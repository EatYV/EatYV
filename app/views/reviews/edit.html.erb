<% if current_user == @review.user %>
  <%= form_for([@cuisine, @review]) do |f| %>
    <div class="field">
      <%= f.label :grades %>
      <%= f.text_field :grades %>
    </div>

    <div class="field">
      <%= f.label :comments %>
      <%= f.number_field :comments, class: :text_field, step: :any %>
    </div>

    <div class="actions">
      <%= f.submit 'Add Review' %>
    </div>
  <% end %>
<% end %>
