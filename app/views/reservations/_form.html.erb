 <% if user_signed_in? %>
  <%= form_for([@cuisine, @cuisine.reservations.build]) do |f| %>
    <div class="field">
      <%= f.label :name %>
      <%= f.text_field :name %>
    </div>
    <br>
    <div class="field">
      <%= f.label :time %>
      <%= f.text_field :time %>
    </div>
  <br>
    <div class="field">
      <%= f.label :message %>
      <%= f.text_area :message %>
    </div>
  <br>
  <%= f.submit %>

  <% end %>
<% end %>
